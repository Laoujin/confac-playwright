{
  "name": "confac-playwright",
  "version": "1.0.0",
  "description": "Playwright tests for confac",
  "type": "module",
  "scripts": {
    "test": "bunx playwright test",
    "test:ci": "bun run infra:full:up && bun run seed && bunx playwright test && bun run infra:full:down",
    "test:e2e": "bunx playwright test --project=e2e",
    "test:entities": "bunx playwright test --project=entities",
    "test:flows": "bunx playwright test --project=flows",
    "test:integrations": "bunx playwright test --project=integrations",
    "test:claims": "bunx playwright test --project=claims",
    "ui": "bunx playwright test --ui",
    "headed": "bunx playwright test --headed",
    "failed": "bunx playwright test --last-failed",
    "debug": "bunx playwright test --debug",
    "report": "bunx playwright show-report",
    "codegen": "bunx playwright codegen localhost:3000",
    "trace": "bunx playwright test --trace on",
    "infra:up": "docker compose -f docker-compose.test.yml up -d --build",
    "infra:down": "docker compose -f docker-compose.test.yml down",
    "infra:logs": "docker compose -f docker-compose.test.yml logs -f",
    "infra:reset": "docker compose -f docker-compose.test.yml down -v && docker compose -f docker-compose.test.yml up -d --build",
    "infra:full:up": "docker compose -f docker-compose.full.yml up -d --build",
    "infra:full:down": "docker compose -f docker-compose.full.yml down",
    "infra:full:logs": "docker compose -f docker-compose.full.yml logs -f",
    "seed": "bun seed/seed.ts",
    "mocks:reset": "curl -X POST http://localhost:3002/reset && curl -X POST http://localhost:3300/reset"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/itenium-be/confac-playwright.git"
  },
  "keywords": [],
  "author": "itenium",
  "license": "MIT",
  "devDependencies": {
    "@faker-js/faker": "^9.8.0",
    "@playwright/test": "^1.53.1",
    "@types/node": "^24.0.3",
    "dotenv": "^16.5.0",
    "mongodb": "^3.7.4",
    "typescript": "^5.0.0"
  }
}
